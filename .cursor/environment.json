{
  "name": "GOG Pub Games",
  "install": "sudo apt-get update && sudo apt-get install -y python3-venv && python3 -m venv venv && ./venv/bin/pip install --upgrade pip -q && ./venv/bin/pip install -r requirements.txt && cd grumpy-token && npm install",
  "terminals": [
    {
      "name": "web",
      "command": "./venv/bin/python -m flask --app app run --host 0.0.0.0 --port 5000"
    }
  ],
  "ports": [5000]
}
